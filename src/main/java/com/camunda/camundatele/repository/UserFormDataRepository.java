/*
package com.camunda.camundatele.repository;

import com.camunda.camundatele.entities.FormData;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

@Repository
public interface UserFormDataRepository extends JpaRepository<FormData, Long> {
  public Optional<FormData> findByProcessInstanceKey(String processInstanceKey);
  public Optional<FormData> findById(String taskId);
}
*/
